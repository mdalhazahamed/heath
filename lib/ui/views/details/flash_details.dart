import 'package:flutter/material.dart';
class FlashDetails extends StatelessWidget {
  const FlashDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.green,);
  }
}